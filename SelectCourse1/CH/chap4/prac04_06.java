package SelectCourse1.CH.chap4;

public class prac04_06 {

    byte b;
    short s;
    int i;
    long l;
    float f;
    double d;
    char c;
    boolean b1, b2;
    
    /*
     * 
     * 
     *   b+10L              long
        (b+i)*l             long
        (b+i)*l+f           float
        s/f + Math.sin(f)   double
        c == 'b'            boolean
        l+1.5f              float
        i<10                boolean
        b1 == (f >= 5.0)    boolean
        b1 && !b2           boolean
     */

     /*
      * 07번 
        1. short에 long 넣으면 오류
        3. int에 long 연산
        4. boolean값에 int값 못넣음
        6. char는 ' ' 안에 넣어서 리터럴로 생성
      */

      /*
       * 08번
       * 100, 200
       * method에서 reference로 받은 b의 값만 2가 곱해지고 primitive type인 a는 변하지 않음
       */
}
